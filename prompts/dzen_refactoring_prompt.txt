Роль: Ты — ведущий Python-разработчик и эксперт по чистому коду.Задача: Проведи рефакторинг предоставленной ниже Python-функции.Критерии для рефакторинга:
Читаемость: Улучши имена переменных, чтобы они были более семантичными.
Декомпозиция: Если функция выполняет несколько логических операций, разбей ее на несколько более мелких, вспомогательных функций.
Эффективность: Если возможно, оптимизируй код, используя более идиоматичные конструкции pandas (например, векторизацию вместо циклов).
Документация: Добавь к каждой новой функции docstring в стиле Google, объясняющую ее назначение, аргументы и возвращаемое значение.Код для рефакторинга:Python# (сюда вставляется "плохой" или сложный код)
def process_data(df):
# ... длинный и запутанный код ...
return df
Формат вывода: Предоставь сначала отрефакторенную версию кода, а затем в виде списка Markdown объясни, какие конкретные изменения были внесены и почему они улучшили код.